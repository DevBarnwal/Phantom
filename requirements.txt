# =============================================================
#  NetSniffer — Requirements
#  Install: pip install -r requirements.txt
# =============================================================

# ── CORE (required now) ───────────────────────────────────────
scapy>=2.4.5                # packet capture & analysis engine
matplotlib>=3.5.0           # live pie + bar charts in side panel

# ── DATA & EXPORT (Phase 2) ───────────────────────────────────
# csv, json, sqlite3 are Python stdlib — no install needed

reportlab>=4.0.0            # PDF session reports
openpyxl>=3.1.0             # Excel (.xlsx) export

# ── GeoIP (Phase 3) ──────────────────────────────────────────
# Requires free GeoLite2 database from https://dev.maxmind.com
geoip2>=4.6.0               # country/city lookup per IP

# ── WEB / REST API (Phase 4) ─────────────────────────────────
flask>=2.3.0                # local REST API server
flask-cors>=4.0.0           # allow browser access to API

# ── SECURITY DETECTION (Phase 4) ─────────────────────────────
# No extra packages — detection logic runs on scapy data

# ── PACKAGING & DISTRIBUTION ─────────────────────────────────
pyinstaller>=6.0.0          # build standalone .exe / binary

# ── OPTIONAL / DEV TOOLS ─────────────────────────────────────
# Uncomment if you want these during development:
# pytest>=7.0.0             # unit testing
# black>=23.0.0             # code formatter
# pylint>=3.0.0             # linter
